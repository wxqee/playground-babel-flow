{
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "npm run flow && npm run build:prod",
    "build:prod": "babel src/ -d dist/",
    "flow": "flow"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-flow": "^7.0.0",
    "flow-bin": "^0.110.0"
  }
}
